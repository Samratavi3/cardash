Act as an expert Frontend Architect and WebGL Engineer specializing in React Three Fiber (R3F), @react-three/drei, and modern automotive UI/UX design. 

I want to build a high-fidelity, interactive 3D Car Dashboard and Configurator showcase model, heavily inspired by Tesla's split-screen layout and premium 3D vehicle viewing platforms like CarDekho. This is a highly visual, animated UI model project (no real car-hardware functionality required).

Please generate a modular, production-ready codebase using:
- Framework: React (Vite)
- 3D/Graphics: Three.js, React Three Fiber (@react-three/fiber), @react-three/drei, @react-three/postprocessing
- Styling: Tailwind CSS
- Animation: Framer Motion (for smooth 2D UI panels) & GSAP or Drei's camera controls (for smooth 3D interpolation)
- Icons: Lucide React

### 1. Screen Layout (Strict 3-Part Division)
The screen must be a fixed, full-bleed viewport wrapper (`h-screen w-screen overflow-hidden bg-neutral-950 text-white font-sans select-none`). Divide it into these exact regions:
- LEFT PANEL (1/3 Width - Driver Cluster): A persistent, data-dense digital instrument cluster. It must include a functional-looking speedometer (with an interval timer simulating a smooth speed float around 65 mph), a battery gauge arc (showing 88% range remaining), current gear indicator (D), mock adaptive cruise control/lane-keeping visualization graphics, and active vehicle status alerts.
- CENTER & RIGHT REGION (2/3 Width - 3D Environment Canvas): A massive, uninterrupted WebGL canvas area hosting our hyper-realistic 3D vehicle model.
- BOTTOM HORIZONTAL DOCK: A clean glassmorphic floating bar tracking global status like media playback controls ("Cyberdrive - Synthwave 2026"), responsive dual-zone climate sliders, and a settings profile shortcut.

### 2. 3D Model Setup & Realism (CarDekho Style)
- Asset Loading: Use Drei's `useGLTF` hook to load a high-quality, free, public GLTF model of a recognizable car (e.g., a modern sport sedan or electric vehicle chassis placeholder URL). Ensure you include a `<Suspense>` fallback loader component with a glowing spinner.
- Visual Realism & Lighting:
  * Setup a professional studio environment layout: Key light with casting shadows, a soft fill light, and a sharp cyan or white rim light to define the car's body silhouettes.
  * Embed an Environment Map component (`<Environment preset="studio" />`) to produce hyper-realistic, glossy metallic reflections across the car's paint job.
  * Ground plane: Provide a dark, reflective floor plane (`meshStandardMaterial` with roughness 0.1, metalness 0.8) along with a subtle sci-fi grid overlay to enhance depth perception.
  * Post-Processing: Include a post-processing pipeline using `<EffectComposer>` with mild `Bloom` (making headlights, taillights, and active components glow vividly) and SSR (Screen Space Reflections) or Ambient Occlusion if possible.

### 3. Raycasting, Hotspots, and Context-Aware Animations
Implement direct `onPointerDown`/`onClick` raycasting directly onto the specific sub-meshes of the 3D car model to trigger individual, interactive workflow animations:
- THE DOORS: Clicking a car door mesh must trigger a smooth swing-open animation (interpolating the door object's rotation via a ref on frame updates). Simultaneously, it must slide open a glassmorphic 2D overlay drawer from the side labeled "Door Management" containing interactive buttons for [Lock/Unlock], [Window Up/Down], and [Vent Skylight].
- THE SIDE MIRRORS: Clicking the side mirror mesh must capture focus. The camera must smoothly interpolate (using a lerp or `CameraControls`) to a close-up cinematic angle focusing directly on that mirror. A floating 2D directional D-pad [▲ ▼ ◀ ▶] UI menu must fade in directly adjacent to it, simulating physical mirror adjustment.
- INTERIOR / EXTERIOR VIEWS: Provide a prominent, floating toggle button hovering over the canvas zone. When clicked, it must smoothly transition the camera *inside* the vehicle cabin interior (changing position vector coordinates and widening the camera FOV field-of-view for a spacious 360 look-around perspective). Clicking back must return it seamlessly to the external orbit path.

### 4. Code Architecture Guidelines
Deliver clean, decoupled, and highly modular React component code files structure:
1. `App.jsx` - Main UI layout controller, maintaining global states (active camera view, door state, open settings, selected mesh part context).
2. `DriverCluster.jsx` - Renders the standalone 1/3 driver telematics deck on the left.
3. `CarCanvas.jsx` - Houses the `<Canvas>` pipeline, shadows, environment reflections, camera lerping logic, and post-effects.
4. `VehicleModel.jsx` - Manages the GLTF model parse layout, maps highly polished physical materials (metallic paint roughness 0.2, glass transmission 0.9, alloy wheels metalness 1.0), and hooks up the interaction raycasters on doors/mirrors.
5. `ControlOverlay.jsx` - Builds out the contextual floating 2D UI dashboards utilizing Framer Motion for elegant, ultra-premium glassmorphism cards (`bg-neutral-900/60 backdrop-blur-xl border border-neutral-800/50 rounded-2xl`).

Let's construct this complete interactive interface model now.